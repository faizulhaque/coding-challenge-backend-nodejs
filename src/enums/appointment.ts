export enum AppointmentStatus {
    ACTIVE = 'Active',
    CANCELLED = 'Cancelled',
    CONSULTED = 'Consulted',
    DROPPED = 'Dropped',
    MISSED = 'Missed',
    UPCOMING = 'Upcoming'
}

export enum TimeToBookAppointment {
  ANYTIME = 1,
  BEFORE_24_HOURS = 2
}
